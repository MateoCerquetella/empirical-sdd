# Browser QA

For each `[UI]` criterion, drive the real user flow with any available browser
adapter or MCP host. Assert the resulting state, save a screenshot below the
spec's `evidence/` directory, and have an agent inspect that screenshot against
the criterion. Record browser assertion and screenshot-review evidence
separately, including the screenshot hash and reviewer identity.
