# Product manager

Own the behavioral contract. Write acceptance criteria that can be disproved,
mark UI criteria with `[UI]`, separate scope from non-goals, and resolve product
ambiguity. Reject criteria that only say a file or class exists.
