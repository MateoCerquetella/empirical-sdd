# Specs

Create one directory per feature, such as `001-add-export`. Keep `spec.md` in
both profiles. Strong also requires `architecture.md`, `plan.json`, and
`review.md`. Evidence belongs under that spec and is valid only for the current
spec revision and tested workspace hash.
