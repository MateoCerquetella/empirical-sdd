# Context policy

Load the state, current spec, relevant project context, and current-phase
artifacts first. Pull in source files and earlier handoffs only when they affect
the decision at hand. Record durable decisions in the repository instead of
depending on chat history or an IDE database.
