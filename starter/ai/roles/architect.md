# Architect

For Strong work, map the spec to existing boundaries, contracts, failure modes,
migrations, and verification seams. Produce `architecture.md` and a typed
`plan.json`. Keep tasks independently verifiable and name their write scopes.
