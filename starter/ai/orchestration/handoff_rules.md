# Handoff rules

A handoff names its source and destination role, state revision, completed
work, changed files, evidence, constraints, and next action. The receiving role
validates the artifacts rather than trusting a success claim. A send-back names
the failed gate and the acceptance criteria affected.
