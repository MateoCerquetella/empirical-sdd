# Plan

1. Replace Cargo metadata with an npm package and Bun development scripts.
2. Implement types, filesystem storage, workflow transitions, evidence gates,
   and v1 adoption in the shared core.
3. Implement the global CLI and stdio MCP tool surface.
4. Add non-destructive project discovery and MCP configuration adapters.
5. Replace Rust distribution, documentation, installers, and CI.
6. Test Quick, Strong, concurrency, adoption, MCP, Node runtime, and packaging.
