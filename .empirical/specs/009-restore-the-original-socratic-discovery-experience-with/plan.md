# Plan

1. Add `src/discovery.ts` with the persisted record types, safe identifier, domain-aware five passes, bounded follow-up logic, recommendation, refined request, Markdown rendering, and atomic symlink-safe writes.
2. Replace the CLI's human Explore branch with TTY-aware mode selection and a readline interview supporting blank-answer retry, `:quit`, restart, approval, save-only, conservative workflow selection, and optional shell-free Codex launch.
3. Keep `--json`, `--no-interview`, non-TTY, MCP, and TypeScript paths on the unchanged pure packet API, and document explicit option conflicts.
4. Upgrade every managed instruction/skill/command template from generic exploration to the restored five-pass, one-question-at-a-time approval contract.
5. Add focused tests for pure question/follow-up behavior, full piped interactive Complex handoff and persistence, rejected/save-only behavior, pure packet immutability, integration refresh, and safe discovery paths.
6. Update CLI help, README, protocol/demo/architecture/comparison documentation, package version, and built smoke expectations for 2.3.0.
7. Run typecheck and focused tests, exercise an actual disposable game interview, run the complete release gate, review the diff criterion by criterion, and require Ubuntu/macOS/Windows CI before delivery.
