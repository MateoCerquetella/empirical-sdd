# Plan

1. Extend public types and the core engine with the Fast profile, generated
   minimal criteria, combined evidence/review validation, safe failure
   escalation, and the cooperative Loop operation.
2. Extend the CLI and MCP schemas with Fast and Loop, update help and runtime
   messaging, and preserve the boundary that the caller executes code.
3. Extend project integration generation with automatic Agent Skills and
   native host command fallbacks using ownership-aware, idempotent writes.
4. Add core and MCP regression tests for Fast, Loop, generated integrations,
   conflict preservation, and package-version parity while retaining existing
   Quick and Strong assertions.
5. Update README and protocol, architecture, MCP, demo, migration, and security
   documentation; remove the required “Use Empirical” prompt language.
6. Build and run the full CI command, inspect the packed artifact, initialize a
   clean demo repository twice, and validate the generated files and a complete
   Fast hello flow.
